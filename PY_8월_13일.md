
```py
def test_simple():
    assert class_total([{'국어': 10, 'b': 20, 'c': 30}])

def test_simple():
    scores1 = {'국어': 10, 'b': 20, 'c': 30}
    scores2 = {'국어': 10, 'b': 20, 'c': 30}
    class_scores = [scores1, scores2]
    assert class_total()

def test_simple():
assert class_total([{'국어': 10, 'b': 20, 'c': 30}], '국어') = 10
```